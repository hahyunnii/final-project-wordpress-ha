# Terraform state — contains sensitive values including DB password
terraform.tfstate
terraform.tfstate.backup
terraform.tfstate.d/

# Saved plan output
plan.out

# Terraform working directory and lock file
.terraform/
.terraform.lock.hcl

# Variable overrides — db_master_password must never be committed
terraform.tfvars
*.auto.tfvars

# SSH private keys
*.pem

# OS / editor artifacts
.DS_Store
*.swp
*.swo
